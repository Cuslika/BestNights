<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.bestnights">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TicketsActivity" />
        <activity android:name=".FestivalListActivity" />
        <activity android:name=".PartyListActivity" />
        <activity android:name=".PartyListActivityAdmin" />
        <activity android:name=".FestivalListActivityAdmin" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DecisionActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>